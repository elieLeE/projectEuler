#ifndef _NBRE_H_
#define _NBRE_H_

#include <stdio.h>
#include <stdbool.h>
#include "donnees.h"

bool isPrime(int nbre);
long phiListe(int n, int maxPrime, liste l);
liste searchDeb(int n, liste l);

#endif
